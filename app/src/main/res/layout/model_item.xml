<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:bind="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    >
    <data>
        <import type="android.view.View"/>
        <variable
            name="model"
            type="com.example.sefe.myapplication.viewmodels.SampleModel">
        </variable>
    </data>
    <android.support.v7.widget.CardView
        android:layout_margin="2dp"
        android:id="@+id/itemRoot"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
        <LinearLayout
            android:background="#2222DD"
            android:onClick="@{(view) -> model.clicked(view)}"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
            <Button
                android:layout_gravity="right"
                android:visibility="gone"
                android:text="o"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />
            <TextView
                android:textSize="14dp"
                android:textColor="#FFFFFF"
                android:padding="10dp"
                android:layout_gravity="left"
                android:text="@{model.toString(),default=hej}"
                android:layout_width="match_parent"
                android:layout_height="wrap_content" />
        </LinearLayout>
        <LinearLayout
            android:visibility="@{model.ChildrenVisible ? View.VISIBLE : View.GONE}"
            bind:litem="@{model}"
            android:id="@+id/item_properties"
            android:orientation="vertical"
            android:layout_gravity="bottom"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>
        </LinearLayout>
    </android.support.v7.widget.CardView>
</layout>
